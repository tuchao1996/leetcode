#pragma once
#include<vector>
using namespace std;

class lcode20180821
{
public:
	lcode20180821();
	~lcode20180821();
};

class canPlaceFlowers_problem {
	bool canPlaceFlowers(vector<int>& flowerbed, int n);
};

class findPoisonedDuration_problem {
	int findPoisonedDuration(vector<int>& timeSeries, int duration);
};

class asteroidCollision_problem {
	vector<int> asteroidCollision(vector<int>& asteroids);
};